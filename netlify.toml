[build]
  publish = "dist/angular-web3-wallet/browser"
  command = "npm run build"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
